{
  "name": "ultravelozmente",
  "version": "2.0.0",
  "private": true,
  "description": "Sitio WorldBrain México — Eleventy static site",
  "scripts": {
    "dev": "eleventy --serve --quiet",
    "build": "eleventy",
    "check:external": "grep -rn 'randomuser\\|pravatar' src/ && exit 1 || echo '✓ Sin dependencias de imágenes externas'"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
